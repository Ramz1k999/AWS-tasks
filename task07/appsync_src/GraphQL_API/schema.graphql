type Event {
	id: ID!
	userId: Int!
	createdAt: String!
	payLoad: PayLoadType!
}

type Query {
	getEvent(id: ID!): Event
}

type Mutation {
	createEvent(userId: Int!, payLoad: AWSJSON!): Event
}

schema {
	query: Query
	mutation: Mutation
}